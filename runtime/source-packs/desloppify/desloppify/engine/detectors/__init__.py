"""Language-agnostic detector package.

Modules in this package provide:
- detector implementations (`detect_*` entrypoints)
- shared detector datatypes/contracts (`base.py`)
- optional command adapters where relevant
"""
