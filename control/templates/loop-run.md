# Loop-Run Template

## Batched loop run YYYY-MM-X - short label

Run scope:
- bounded loop family
- current authority surfaces only
- explicit stop condition

Verified micro-fixes:
- `path/to/file`: concise verified fix summary.
- `path/to/file`: concise verified fix summary.

Verification run:
- focused `rg` / `grep`
- `npm run report:directive-workspace-state`
- `npm run check`

Stop summary:
- why the loop stopped
- what remains, if anything
- why the remaining work is out of scope, lower ROI, or no longer clearly dominant
