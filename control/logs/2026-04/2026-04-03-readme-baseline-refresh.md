# 2026-04-03 - README baseline refresh

## Summary

- refreshed the root README to describe the current simplified v1 operating baseline instead of only the older setup trajectory
- refreshed `shared/README.md` so the shared surface description matches the current canonical state/path/navigation libraries

## Operator effect

- the root README now tells readers what has already been achieved and what the repo currently optimizes for
- the shared surface README now reflects the real canonical compatibility and navigation helpers instead of an older narrower library list

## Verification

- `npm run check:control-authority` passed
- `npm run check` passed
